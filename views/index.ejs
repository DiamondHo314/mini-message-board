<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/homeStyles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= title %></title>
</head>
<body>
   <h1 id="heading"> <%= title %></h1> 
   <div id="board">
        <% messages.forEach((msg) => { %>  
                <% msgUser = msg.user %>
                <a id="messageFrom" href="/messageDetails/<%= msgUser %>"> from <%= msg.user %> </a>
                <br>
        <% }) %>
   </div>
   <footer>
    <a href="/">home</a>
    <a href="/form">leave a message</a>
   </footer>
</body>
</html>